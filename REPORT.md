# MiniSeckill Mac ARM64 同机优化对比压测报告

测试日期：2026-07-13（Asia/Shanghai）
测试类型：单机、真实容器环境、恒定到达率压测
对比对象：同一台 Mac 上的优化前基线与优化后版本

> [!IMPORTANT]
> **跨机器数据不可直接比较。** 旧报告记录的是 Windows 11 / AMD Ryzen 7 6800H 环境，本报告使用 macOS / Apple M1。旧记录中的 k6 v2.0.0、Docker Compose v5.1.4 均为当时真实版本，但不参与本报告的性能增减计算。本文所有“基线 / 优化后 / Δ”只来自本机同参数复测，唯一预期变量是应用代码。

## 1. 结论摘要

- 三个主场景均达到既定到达率，三轮中位数的 `system_error_rate` 均为 0，未出现 dropped iterations。
- 不同用户洪峰中，成功入队响应 p95 从 **8.17 ms** 降至 **5.49 ms**，下降 **32.8%**；p99 下降 **38.2%**。
- 重复请求场景中，成功入队响应 p95 从 **7.92 ms** 降至 **4.76 ms**，下降 **39.9%**。
- 售罄尾部中，整体 HTTP p99 从 **98.27 ms** 降至 **12.04 ms**，下降 **87.7%**；成功入队响应 p95 下降 **47.4%**。
- “限流 + anti-brush 全开”单轮对照中，约 20 分钟观测窗口的 MySQL `Com_select` 增量下降 **23.5%**；目标 SQL digest 中，动态限流规则查询下降 **99.6%**，用户订单事实查询下降 **77.8%**。
- 同一优化版关闭异步日志后，成功入队响应 p95 为 **14.47 ms**；开启后为 **5.49 ms**，下降 **62.1%**，证明高频入口日志异步化对尾延迟有直接收益。
- 所有正式轮次的 MySQL 成功订单数均未超过初始库存，用户订单唯一键重复组均为 0。个别轮次出现“入口受理数略高于最终成功数”，由 MySQL 条件扣库存挡回并记录失败订单；报告不把“数据库零超卖”夸写成“入口受理永远恰好等于库存”。

## 2. 测试环境

| 项目 | 本机实测值 |
|---|---|
| OS | macOS 26.5.1（Build 25F80） |
| 架构 / CPU | arm64 / Apple M1，8 核 |
| 内存 | 16 GiB |
| Docker | Client 29.5.3 / Server 29.5.3 |
| Docker Compose | v5.1.4 |
| k6 | v2.1.0，darwin/arm64 |
| 应用 Java | Eclipse Temurin OpenJDK 17.0.15 |
| Spring Boot | 3.3.5 |
| MySQL | 8.0.46，aarch64 |
| Redis | 7.4.9 |
| RabbitMQ | 3.13.7 |
| 应用端口 | `18080` |

基础组件均运行在 Docker 中；优化版应用容器为 `mini-seckill-app-opt`。压测使用 `$HOME/bin/k6`，由 `benchmark/run-k6.sh` 显式传入，避免误用 PATH 中的其他版本。

## 3. 公平性与统计口径

### 3.1 三个主场景

基线和优化版均使用同一套 `benchmark/run-suite.sh`：

| 场景 | 模式 | 到达率 | 时长 | 初始库存 | 用户口径 |
|---|---:|---:|---:|---:|---|
| 不同用户洪峰 | unique | 75 iterations/s | 60s | 1000 | 每次迭代不同用户 |
| 重复请求防重 | duplicate | 75 iterations/s | 60s | 1000 | 1000 个用户循环请求 |
| 售罄尾部 | unique | 300 iterations/s | 30s | 100 | 每次迭代不同用户 |

每个版本、每个场景连续跑 3 次，表中延迟取三轮逐指标中位数。每轮开始前都执行：

1. `benchmark/reset.sql` 清理订单、消息、日志、补偿、动态规则和库存表；
2. 清理 Redis `seckill:*` key；
3. purge RabbitMQ order/dead 队列；
4. 通过应用接口重新初始化并预热相同库存。

### 3.2 HTTP 请求数为什么高于 iterations

每次迭代先请求 token；拿到 token 后才请求下单接口。库存售罄后，大量请求会在 token 阶段快速结束，因此 `http_reqs` 是 token 与下单请求之和，不能直接当成“下单吞吐”。公平性主要看恒定到达率是否完成、系统错误、业务分类、延迟与最终对账。

### 3.3 开关口径

- 主场景：两版都关闭业务限流、anti-brush 和动态限流，用于隔离订单链路本身。
- 全开对照：两版都开启限流、anti-brush、动态限流并使用隐藏下单 path；各跑 1 次，专门观察 D2/D3 的数据库访问变化。
- 异步日志对照：同一个优化 jar，仅切换 `seckill.async-log.enabled`；关闭组跑 3 次，与优化版开启组的三轮中位数比较。

## 4. 三场景三轮中位数

### 4.1 不同用户洪峰：75/s × 60s，库存 1000

| 指标 | 基线 | 优化后 | Δ |
|---|---:|---:|---:|
| 完成 iterations | 4501 | 4501 | 持平 |
| `system_error_rate` | 0 | 0 | 持平 |
| 整体 HTTP p50 | 3.93 ms | 3.62 ms | -7.9% |
| 整体 HTTP p95 | 6.69 ms | 4.89 ms | -26.9% |
| 整体 HTTP p99 | 15.26 ms | 7.14 ms | -53.2% |
| 成功入队 p50 | 5.64 ms | 3.79 ms | -32.9% |
| 成功入队 p95 | 8.17 ms | 5.49 ms | -32.8% |
| 成功入队 p99 | 13.97 ms | 8.64 ms | -38.2% |
| 入队数中位数 | 1000 | 1000 | 持平 |
| 售罄分类中位数 | 3500 | 3500 | 持平 |

结论：在相同到达率下，优化没有靠拒绝请求换性能，主要收益集中在成功入队链路和尾延迟。

### 4.2 重复请求：75/s × 60s，1000 用户，库存 1000

| 指标 | 基线 | 优化后 | Δ |
|---|---:|---:|---:|
| 完成 iterations | 4501 | 4500 | 约持平 |
| `system_error_rate` | 0 | 0 | 持平 |
| 整体 HTTP p50 | 3.66 ms | 3.49 ms | -4.7% |
| 整体 HTTP p95 | 6.23 ms | 4.70 ms | -24.6% |
| 整体 HTTP p99 | 8.17 ms | 5.72 ms | -29.9% |
| 成功入队 p50 | 5.64 ms | 3.66 ms | -35.2% |
| 成功入队 p95 | 7.92 ms | 4.76 ms | -39.9% |
| 成功入队 p99 | 10.68 ms | 6.99 ms | -34.6% |
| 入队数中位数 | 1000 | 1000 | 持平 |
| 重复请求中位数 | 3501 | 3500 | 约持平 |

结论：Redis 幂等和数据库唯一约束仍保证每个用户最多一单；入口链路优化降低了重复流量下的尾延迟。

### 4.3 售罄尾部：300/s × 30s，库存 100

| 指标 | 基线 | 优化后 | Δ |
|---|---:|---:|---:|
| 完成 iterations | 9001 | 9000 | 约持平 |
| `system_error_rate` | 0 | 0 | 持平 |
| 整体 HTTP p50 | 2.09 ms | 2.12 ms | +1.1% |
| 整体 HTTP p95 | 6.93 ms | 3.09 ms | -55.4% |
| 整体 HTTP p99 | 98.27 ms | 12.04 ms | -87.7% |
| 成功入队 p50 | 24.33 ms | 12.21 ms | -49.8% |
| 成功入队 p95 | 39.51 ms | 20.79 ms | -47.4% |
| 成功入队 p99 | 44.70 ms | 27.11 ms | -39.3% |
| 入队数中位数 | 100 | 100 | 持平 |
| 售罄分类中位数 | 8901 | 8900 | 约持平 |

结论：p50 基本不变，p95/p99 大幅下降，与 A3 减少分片库存 Redis 往返、加快售罄判断的预期一致。该结果是基线与完整优化版的混合版本对比，不作为 A3 单点开关实验。

## 5. 限流与 anti-brush 全开对照

该组为单轮对照，不与三轮中位数混用。两版参数均为 duplicate 75/s × 60s、库存 1000、1000 用户、隐藏 path；两版最终均为 1000 个成功订单、0 个重复订单组、系统错误率 0。

### 5.1 延迟

| 指标 | 基线 | 优化后 | Δ |
|---|---:|---:|---:|
| 整体 HTTP p50 | 4.10 ms | 3.82 ms | -6.8% |
| 整体 HTTP p95 | 14.37 ms | 8.97 ms | -37.6% |
| 整体 HTTP p99 | 56.01 ms | 44.25 ms | -21.0% |
| 成功入队 p50 | 8.17 ms | 6.28 ms | -23.2% |
| 成功入队 p95 | 43.32 ms | 35.98 ms | -16.9% |
| 成功入队 p99 | 176.41 ms | 124.47 ms | -29.4% |

### 5.2 MySQL 观测窗口增量

DB metrics 的 before/after 采样窗口约 20 分钟，包含 60 秒 k6 压测、等待最终对账和后台定时任务，不是纯 60 秒 SQL 计数。两个目标 digest 的增量与请求路径数量严格吻合，可用于说明 D2/D3；`Com_select` 只能解释为近似等长观测窗口的总查询差异。

| 指标 | 基线增量 | 优化后增量 | Δ |
|---|---:|---:|---:|
| `Com_select` | 23,422 | 17,920 | -23.5% |
| 动态限流规则查询 | 1,999 | 7 | -99.6% |
| 用户订单事实查询 | 4,501 | 1,000 | -77.8% |

归因：

- D2 使用 `Optional` 哨兵缓存“无动态规则”结果，5 秒 TTL 内不再每次回源 MySQL，因此规则查询从近 2000 次降到 7 次。
- D3 在 token 热路径先查 Redis 订单状态和用户 SKU 幂等 key，只在 Redis 无事实时查 MySQL，因此订单事实查询减少 3501 次。
- 总 `Com_select` 没有按上述两项简单相加下降，因为观测窗口还包含活动、库存、消息状态查询和后台任务；报告只陈述近似等长窗口实测总量，不把它包装成纯 60 秒压测值。

## 6. 异步日志开关对照

对比对象是同一优化版 jar。关闭异步日志的三轮中位数，与主场景中开启异步日志的三轮中位数如下：

| 指标 | 异步关闭 | 异步开启 | Δ |
|---|---:|---:|---:|
| 整体 HTTP p50 | 3.92 ms | 3.62 ms | -7.6% |
| 整体 HTTP p95 | 7.46 ms | 4.89 ms | -34.4% |
| 整体 HTTP p99 | 15.73 ms | 7.14 ms | -54.6% |
| 成功入队 p50 | 6.19 ms | 3.79 ms | -38.9% |
| 成功入队 p95 | 14.47 ms | 5.49 ms | -62.1% |
| 成功入队 p99 | 26.40 ms | 8.64 ms | -67.3% |

D4 的收益主要体现在成功入队的尾延迟：高频业务日志不再同步占用请求线程和数据库连接。异步线程池为有界队列，并暴露接收、完成、拒绝等指标；需要回滚时可将 `seckill.async-log.enabled=false` 切回同步写。

关闭异步日志的第 1 轮入口受理 1003 次，最终形成 1000 个成功订单 + 3 个 FAILED 订单，1003 条消息进入消费终态，并触发库存汇总/Redis 修复补偿；第 2、3 轮为 1000 个成功订单且消息闭合。开关结论使用三轮逐指标中位数，异常轮次仍完整保留在稳定证据中。

## 7. 正确性与异常口径

### 7.1 稳定成立的断言

- 所有主场景轮次的 MySQL `success_orders` 都不超过初始库存；库存 1000 场景最终成功 1000 单，库存 100 场景最终成功 100 单。
- 所有轮次 `duplicate_order_groups=0`。
- 优化版正常轮次 RabbitMQ order/dead 队列最终均为 0。
- 优化版出现 MySQL 库存保护拒绝时，会持久化 `FAILED` 订单行；因此“消费者已处理消息”和“成功订单数”可以不同，但失败结果不会只存在于短 TTL 日志或 Redis key 中。

### 7.2 个别轮次的真实异常

- 基线 unique 第 1 轮入口受理 1005 次，最终成功 1000 单，留下 1 条 dead queue 消息及非最终消息状态。该轮消息闭环不完整；现有证据没有单独复现并定位其触发原因。
- 优化版 unique 第 3 轮入口受理 1002 次，最终为 1000 个成功订单 + 2 个失败订单；1002 条消息均进入消费终态，数据库仍未超卖。这里的改进是失败可追踪、消息与订单总记录闭合，而不是宣称入口绝不多受理。
- 优化版 async-off unique 第 1 轮入口受理 1003 次，最终为 1000 个成功订单 + 3 个失败订单；1003 条消息进入消费终态，并产生库存汇总/Redis 修复补偿。该轮纳入 D4 三轮中位数，没有被选择性删除。
- 开启 MySQL 分段库存后，`sku_stock_segment` 是扣减事实，`sku_stock` 汇总表由 60 秒对账任务同步。压测结束恰好撞上同步窗口时，汇总表快照可能暂时滞后；正确性判断使用分段库存合计、成功订单数、唯一键和消息终态，不把瞬时汇总值单独当作最终结论。

## 8. 各优化点的证据边界

| 优化点 | 本报告可观测结果 | 不能从本报告单独证明的内容 |
|---|---|---|
| D1 雪花订单 ID | 单机多轮无撞键；另有四实例实测，workerId 0–3 均出单且 `order_id` 重复为 0 | 四实例单机容器实测仍不等于跨主机长期运行；详见 [REPORT-RELIABILITY.md](REPORT-RELIABILITY.md) |
| D2 动态规则负缓存 | 规则查询 1999 → 7 | 不代表所有动态规则场景都不访问数据库，缓存失效后仍会回源 |
| D3 Redis-first 查重 | 用户订单事实查询 4501 → 1000 | Redis miss 时仍需查 MySQL，这是正确兜底 |
| D4 高频日志异步化 | 成功入队 p95 下降 62.1%（同 jar 开关对照） | 不能据此声称所有数据库写入都已异步化；本地消息和订单事实仍同步保证可靠性 |
| D5 消费异常分级 | 优化版消息能收敛到消费终态，失败有记录 | 本轮未主动注入数据库死锁或连接中断 |
| D6 订单状态事实化 | 查询结果不再依赖日志字符串 | 性能收益不是本轮主要观测目标 |
| D7 XFF 信任边界 | 默认不信任转发头，配置可回滚 | 单机压测未覆盖真实代理链攻击 |
| A1 滑动窗口限流 | 全开场景功能正常、系统错误率 0 | 本报告没有单列固定窗口与滑动窗口的边界突发 AB；临界语义由 Lua 单测覆盖 |
| A2 并发零超卖 IT | 数据库成功订单始终不超过库存；CI 实跑 Testcontainers；另有四实例、Redis 故障和消息积压演练 | 本地演练不能替代生产容量与跨主机故障验证；详见 [REPORT-RELIABILITY.md](REPORT-RELIABILITY.md) |
| A3 分片单 Lua | 售罄尾部 HTTP p99 下降 87.7%；Redis 180 秒故障后按 MySQL 事实重建并继续放量 | 仍是单机 Redis，不等同于 Redis Cluster 故障转移；详见 [REPORT-RELIABILITY.md](REPORT-RELIABILITY.md) |
| A4 超时释放幂等 key | 代码闭合 10 分钟超时后的重试语义 | 本轮 30/60 秒压测不会触发 10 分钟超时 |
| A5 删除 `version` | 全部场景在条件更新下保持数据库零超卖 | 并发安全来自 `available_stock > 0` 条件更新，不应归因于已删除的乐观锁字段 |

## 9. 主场景运行命令与证据

下面的 suite 命令只负责运行场景，**不会自动切换 jar**。每组运行前必须先替换应用容器中的 jar，并保持相同启动参数。阶段 3 使用的文件及 SHA-256：

| 版本 | 本地 jar | SHA-256 |
|---|---|---|
| 基线 | `/tmp/mini-seckill-baseline.jar` | `b69dc089a56e4c6c8749f96d354d036dbd12ad1c976c5e9dabcb18fc6754e623` |
| 优化后 | `/tmp/mini-seckill-a15.jar` | `093b396d7b74c5cf4a8487badc927284b036a9f9573bd5efe8b4c062186be84a` |

主场景应用启动参数固定为：

```text
--server.port=18080
--spring.profiles.active=100k
--seckill.rate-limit.enabled=false
--seckill.anti-brush.enabled=false
--seckill.dynamic-rate-limit.enabled=false
```

容器还固定使用 `MINI_SECKILL_WORKER_ID=7`，并连接同一组 MySQL、Redis、RabbitMQ 容器。确认 `/actuator/health` 正常后再分别运行三轮：

```bash
BASE_URL=http://localhost:18080 K6="$HOME/bin/k6" bash benchmark/run-suite.sh baseline-mac-r1
BASE_URL=http://localhost:18080 K6="$HOME/bin/k6" bash benchmark/run-suite.sh baseline-mac-r2
BASE_URL=http://localhost:18080 K6="$HOME/bin/k6" bash benchmark/run-suite.sh baseline-mac-r3

BASE_URL=http://localhost:18080 K6="$HOME/bin/k6" bash benchmark/run-suite.sh optimized-mac-r1
BASE_URL=http://localhost:18080 K6="$HOME/bin/k6" bash benchmark/run-suite.sh optimized-mac-r2
BASE_URL=http://localhost:18080 K6="$HOME/bin/k6" bash benchmark/run-suite.sh optimized-mac-r3
```

原始结果保存在 `benchmark/results/`，稳定精选证据保存在 `benchmark/evidence/mac-arm64/`。重点原始文件前缀：

- `20260713-*-baseline-mac-r[123]-{unique,duplicate,selltail}-*`
- `20260713-*-optimized-mac-r[123]-{unique,duplicate,selltail}-*`
- `20260713-*-baseline-mac-all-on-*`
- `20260713-*-optimized-mac-all-on-*`
- `20260713-*-optimized-mac-async-off-r[123]-unique-*`

稳定证据的 `runs/` 目录保留 21 个三轮输入 summary JSON，以及 3 个异常轮次 verify 文件，因此公开仓库可以独立重算主场景和 async 开关中位数。全开组和 DB metrics 仍保留单轮原始摘要；若重新采集，建议把 DB before/after 紧贴 k6 起止时间，减少后台任务噪声。

`benchmark/BASELINE.md` 是阶段 0 冻结记录，本轮没有回写或“修正”其中的历史数据。

## 10. 报告边界

- 这是 Apple M1 单机结果，不代表生产集群容量，也不用于推算水平扩容倍数。
- 未持续采集 CPU、磁盘 IO、网络和 GC 时序，因此不宣称已经完成系统级 profiling。
- 全开对照只有单轮，适合证明查询数量级变化，不应包装成严格的置信区间结论。
- 成功入队只表示 Redis 准入、本地消息记录和 MQ 投递链路已受理；用户最终状态必须以订单查询为准。
- 旧 Windows 报告中的版本号和历史结果保留其真实性，但所有优化百分比只使用本机 Mac 基线与优化后数据。
